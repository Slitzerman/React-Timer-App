var React = require('react');
var PropTypes = React.PropTypes;

var Timer = React.createClass({
  render: function() {
    return (
      <div>
        <div>Timer</div>
      </div>
    );
  }
});

module.exports = Timer;
